name: Lint
on: [push, pull_request]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Lint Files
        uses: ThePalaceProject/ci-scripts/.github/actions/lint@${{ env.GITHUB_SHA }}
